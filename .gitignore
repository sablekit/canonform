# dependencies
node_modules/
.pnp
.pnp.js

# next.js / build
.next/
out/
build/
dist/
.turbo

# env (never commit secrets / API keys)
.env
.env*.local
.env.development
.env.production
!.env.example

# vercel
.vercel

# misc
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.DS_Store
coverage/
*.local

# local db / cache artifacts
*.sqlite
*.sqlite3
.cache/

# internal planning / handoff docs — NEVER commit to the public repo
# (Chinese internal notes + Sablekit burner-identity strategy; keep local only).
# The public doc is the English README. Publish a sanitized spec only if you choose.
CLAUDE.md
SPEC.md
TODO.md
/_internal/
